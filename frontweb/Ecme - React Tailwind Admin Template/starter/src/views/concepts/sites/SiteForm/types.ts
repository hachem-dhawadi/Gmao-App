export type SiteFormSchema = {
    name: string
    code: string
    description: string
    address: string
    phone: string
    timezone: string
    is_active: boolean
    geo_lat: string
    geo_lng: string
}
